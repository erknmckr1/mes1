import { createSlice } from "@reduxjs/toolkit";

const workFlowManagementSlice = createSlice({
    name: "flowmanagement",
    initialState: {
        
    },
    reducers: {
        
    }
})


export const {  } = workFlowManagementSlice.actions;
export default workFlowManagementSlice.reducer;
